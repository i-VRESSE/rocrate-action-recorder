# Reuse file

```shell
example/myscript.py --input data/input.txt --output results/output.txt
example/myscript.py --input data/output.txt --output results/output2.txt
```

When file (data/output.txt) that is already mentioned in ro-crate-metadata.json then reuse that entry.

