# Repeat call

I want to run the same script multiple times with different input parameters.

```shell
example/myscript.py --input data/input.txt --output results/output.txt
example/myscript.py --input data/input2.txt --output results/output2.txt
```

And have second call be recorded in the ro-crate-metadata.json file written by the first call.
